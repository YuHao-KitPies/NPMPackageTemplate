import Sub from './core/sub';

function sum(a, b) {
    return a + b;
}
function sum2(a, b) {
    return a + b;
}
/**
 * Sub Function
 * @param {*} a Input a
 * @param {*} b Input b
 * @returns The result of a - b 
 */
function sum3(a, b) {
    return a - b;
}
console.log('HHHHHHX');
console.log(Sub(5, 4));

export { sum, sum2, sum3 };
